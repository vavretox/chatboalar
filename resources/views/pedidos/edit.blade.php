@extends('layouts.admin') @section('title','Pedido '.$pedido->numero_pedido) @section('content')<div class="head"><div><h1>{{ $pedido->numero_pedido }}</h1><p>Detalle completo y seguimiento del pedido.</p></div></div><div class="grid" style="margin-bottom:17px"><div class="detail"><small>CLIENTE</small><strong>{{ $pedido->cliente?->nombre }} · {{ $pedido->cliente?->telefono }}</strong></div><div class="detail"><small>TOTAL</small><strong>${{ number_format((float)$pedido->total,2,',','.') }}</strong></div></div><div class="card" style="margin-bottom:17px"><h3 style="margin-top:0">Productos</h3><table><thead><tr><th>Producto</th><th>Cantidad</th><th>Precio</th><th>Subtotal</th></tr></thead><tbody>@foreach($pedido->detalles as $detalle)<tr><td>{{ $detalle->producto?->nombre }}</td><td>{{ $detalle->cantidad }}</td><td>${{ number_format((float)$detalle->precio_unitario,2,',','.') }}</td><td>${{ number_format((float)$detalle->subtotal,2,',','.') }}</td></tr>@endforeach</tbody></table></div><form class="card" method="POST" action="{{ route('pedidos.update',$pedido) }}">@csrf @method('PUT')<div class="grid"><div><label>Estado</label><select name="estado">@foreach(['pendiente','confirmado','preparando','enviado','entregado','cancelado'] as $estado)<option value="{{ $estado }}" @selected($pedido->estado===$estado)>{{ ucfirst($estado) }}</option>@endforeach</select></div><div><label>Fecha de entrega</label><input type="datetime-local" name="fecha_entrega" value="{{ optional($pedido->fecha_entrega)->format('Y-m-d\TH:i') }}"></div><div class="full"><label>Dirección de entrega</label><textarea name="direccion_entrega">{{ old('direccion_entrega',$pedido->direccion_entrega) }}</textarea></div><div class="full"><label>Notas</label><textarea name="notas">{{ old('notas',$pedido->notas) }}</textarea></div></div><div class="actions"><a class="btn light" href="{{ route('pedidos.index') }}">Cancelar</a><button class="btn">Guardar pedido</button></div></form>@endsection
