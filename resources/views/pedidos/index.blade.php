@extends('layouts.admin') @section('title','Pedidos') @section('content')<div class="head"><div><h1>Pedidos</h1><p>Consulta y actualiza los pedidos recibidos por el chatbot.</p></div></div><form class="filter"><select name="estado" style="max-width:230px"><option value="">Todos los estados</option>@foreach(['pendiente','confirmado','preparando','enviado','entregado','cancelado'] as $estado)<option @selected(request('estado')===$estado)>{{ $estado }}</option>@endforeach</select><button class="btn light">Filtrar</button></form><div class="card" style="padding:0;overflow:hidden"><table><thead><tr><th>Pedido</th><th>Cliente</th><th>Total</th><th>Fecha</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>@forelse($pedidos as $pedido)<tr><td><strong>{{ $pedido->numero_pedido }}</strong></td><td>{{ $pedido->cliente?->nombre }}<br><small>{{ $pedido->cliente?->telefono }}</small></td><td>${{ number_format((float)$pedido->total,2,',','.') }}</td><td>{{ $pedido->created_at->format('d/m/Y H:i') }}</td><td><span class="pill {{ $pedido->estado==='cancelado'?'off':'' }}">{{ ucfirst($pedido->estado) }}</span></td><td><a class="btn light" href="{{ route('pedidos.edit',$pedido) }}">Ver y editar</a></td></tr>@empty<tr><td colspan="6">No hay pedidos registrados.</td></tr>@endforelse</tbody></table></div>@endsection
