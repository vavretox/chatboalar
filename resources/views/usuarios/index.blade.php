@extends('layouts.admin') @section('title','Usuarios') @section('content')<div class="head"><div><h1>Usuarios</h1><p>Personas que pueden operar el sistema.</p></div><a class="btn" href="{{ route('usuarios.create') }}">+ Nuevo usuario</a></div><div class="card" style="padding:0;overflow:hidden"><table><thead><tr><th>Nombre</th><th>Correo</th><th>Perfil</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>@forelse($usuarios as $usuario)<tr><td><strong>{{ $usuario->name }}</strong></td><td>{{ $usuario->email }}</td><td>{{ ucfirst($usuario->role) }}</td><td><span class="pill {{ $usuario->active?'':'off' }}">{{ $usuario->active?'Activo':'Inactivo' }}</span></td><td><div class="row-actions"><a class="btn light" href="{{ route('usuarios.edit',$usuario) }}">Editar</a><form method="POST" action="{{ route('usuarios.destroy',$usuario) }}" onsubmit="return confirm('¿Eliminar este usuario?')">@csrf @method('DELETE')<button class="btn red">Eliminar</button></form></div></td></tr>@empty<tr><td colspan="5">No hay usuarios.</td></tr>@endforelse</tbody></table></div>@endsection
